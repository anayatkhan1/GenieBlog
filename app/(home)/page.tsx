import Generate from "@/components/generate";
const Home = () => {
    return (
        <section className="py-24">
            <div className="container">
                <Generate />
            </div>
        </section>
    );
};

export default Home;
